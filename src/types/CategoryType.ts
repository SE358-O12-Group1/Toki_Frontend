type CategoryType = {
    _id: string;
    name: string;
    image: string;
};

export const initialCategory: CategoryType = {
    _id: '',
    name: '',
    image: ''
};

export default CategoryType;
